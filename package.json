{
  "name": "printflow",
  "private": true,
  "scripts": {
    "dev": "concurrently -n bot,auth,ui -c green,yellow,cyan \"npm start --prefix backend/printflow-bot\" \"npm run dev --prefix frontend/printflow-auth\" \"npm run dev --prefix frontend/printflow-dashboard\""
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
